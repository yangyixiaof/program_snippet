package cn.yyx.research.todos;

public class TodoList {
	
	// TODO source methods should be as one, not distributed in different element lists.
	// TODO direct assignment should give a node to redirect to other list. Just add a Self connection to other list.
	// TODO method return should be modeled as a distinct element, add Self connection to the method.
	
	// TODO Construction method need to be reconsidered method and its class should be a map.
	
	// TODO remember to add virtual branch to every node in only one branch£¬ such as if(){} without else branch.
}
